#ifndef UTILS_HPP
#define UTILS_HPP

#include <vector>
#include <string>

// Fonction pour séparer une chaîne en mots
std::vector<std::string> split_string(const std::string& s);

#endif // UTILS_HPP
